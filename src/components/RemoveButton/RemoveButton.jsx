import React from 'react';
import "./RemoveButton.scss"

const RemoveButton = () => {

    return (
        <button className="remove">
            <span>Remove</span>
        </button>
    );
}

export default RemoveButton;